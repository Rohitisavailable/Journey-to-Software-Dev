import math

radius = float(input("Enter the Radius of the Circle: "))

diameter = radius * 2

area = math.pi * diameter

print(f"The area of the circle is: {round(area, 2)} and the radius is: {radius} and the Diameter is: {diameter}")